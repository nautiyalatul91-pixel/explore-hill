import { t as supabase } from "./client-B99rZN6a.js";
import "./trek-kedarkantha-Jx50rRly.js";
import { t as PageHero } from "./PageHero-B2khC5ZF.js";
import { n as usePackages } from "./packages-De34pvPG.js";
import { t as PackageCard } from "./PackageCard-C00RwxmJ.js";
import { useEffect, useState } from "react";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
//#region src/routes/treks.tsx?tsr-split=component
function TreksPage() {
	const { packages } = usePackages();
	const treks = packages.filter((p) => p.category === "trek");
	const [content, setContent] = useState(null);
	useEffect(() => {
		async function load() {
			const { data } = await supabase.from("settings").select("*").eq("key", "static_pages").maybeSingle();
			if (data && typeof data.value === "object" && data.value !== null) {
				const val = data.value;
				if (val.treks) setContent(val.treks);
			}
		}
		load();
	}, []);
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(PageHero, {
		eyebrow: "Treks",
		title: content?.title ? content.title : /* @__PURE__ */ jsxs(Fragment, { children: ["Walk into the ", /* @__PURE__ */ jsx("span", {
			className: "text-gradient",
			children: "wild Himalayas"
		})] }),
		subtitle: content?.subtitle || "From beginner-friendly weekend climbs to soulful summit pushes — every trek is led by experienced guides and built around small groups.",
		image: content?.image || "/assets/trek-kedarkantha-BKuhAilf.jpg"
	}), /* @__PURE__ */ jsx("section", {
		className: "py-20 sm:py-28 bg-background",
		children: /* @__PURE__ */ jsx("div", {
			className: "mx-auto max-w-7xl px-4 sm:px-6 grid gap-6 sm:grid-cols-2 lg:grid-cols-3",
			children: treks.map((p) => /* @__PURE__ */ jsx(PackageCard, { pkg: p }, p.slug))
		})
	})] });
}
//#endregion
export { TreksPage as component };
